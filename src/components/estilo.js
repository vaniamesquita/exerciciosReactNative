import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    txtG: {
        fontSize: 32,
        textAlign: 'center',
        color: "#000",
        fontWeight: 'bold',
        marginBottom: 30
    }
})